var redirect_url = sessionStorage.redirect_url || "/"
console.log("redirect_url",redirect_url);
console.log("access_token",WB2.Cookie.load()["access_token"]);
//top.location.assign(redirect_url)
//location.assign(redirect_url)
